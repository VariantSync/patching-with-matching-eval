# result_analysis
Collection of python modules for analyzing the result data of our empirical evaluation. 

## Setup 
Under linux, call the setup script to setup a virtual environment, activate it, and download the requirements:
```shell 
./setup.sh 
```

> To deactivate the environment, you can simply execute `deactivate` in the activated terminal. 
