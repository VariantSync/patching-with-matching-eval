# Evaluation Setup for Patching with Matching
This project uses submodules that must be cloned as well. You can do so by either specifying the `recursive` when cloning

```shell
git clone --recursive git@github.com:VariantSync/patching-with-matching-eval.git 
```

or by updating the submodules afterwards:
```shell
git submodules update --init
``` 

